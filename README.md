# YL MCP Servers

一个 MCP (Model Context Protocol) 服务器集合，为 AI 助手提供各种实用工具。

## 📦 包含的服务器

### yl-git-server
一个智能化的 Git 操作服务器，提供自动化的 Git 工作流功能。

**主要功能：**
- 🚀 智能提交：AI 自动分析代码变更并生成规范的 commit 信息
- 📊 状态查看：快速查看 Git 仓库状态和变更内容
- 🔧 自动化操作：一键完成 add、commit、push 流程
- 🎯 仓库初始化：支持快速初始化 Git 仓库并配置远程地址

[查看详细文档](./yl-git-server/README.md)

## 🚀 快速开始

### 1. 安装依赖

```bash
cd yl-git-server
npm install
```

### 2. 配置 MCP

在 Windsurf 中添加 MCP 服务器配置：

```json
{
  "mcpServers": {
    "yl-git-server": {
      "command": "node",
      "args": ["/Users/mac/Desktop/code/yl-mcps/yl-git-server/src/index.js"]
    }
  }
}
```

详细配置步骤请参考：[MCP 配置指南](./MCP_SETUP.md)

### 3. 开始使用

配置完成后，在 Windsurf 的 Cascade 中即可使用：

```
帮我智能提交代码
```

AI 会自动分析你的代码改动，生成合适的 commit 信息并完成提交。

## 📖 使用示例

### 智能提交代码
```
帮我智能提交代码
```

### 查看 Git 状态
```
查看当前 Git 状态
```

### 初始化仓库
```
初始化一个新的 Git 仓库，远程地址为 git@github.com:username/repo.git
```

## 🛠️ 开发

### 测试服务器

```bash
cd yl-git-server
npm test
```

### 启动服务器

```bash
npm start
```

## 📁 项目结构

```
yl-mcps/
├── yl-git-server/          # Git 操作 MCP 服务器
│   ├── src/
│   │   ├── index.js        # 服务器入口
│   │   ├── server.js       # MCP 服务器实现
│   │   ├── config.js       # 配置
│   │   ├── tool-definitions.js  # 工具定义
│   │   └── tool-handlers.js     # 工具处理器
│   ├── package.json
│   └── README.md
├── mcp_config.json         # MCP 配置文件
├── MCP_SETUP.md           # MCP 配置指南
└── README.md              # 本文件
```

## 🔧 技术栈

- **Node.js**: 运行环境
- **@modelcontextprotocol/sdk**: MCP SDK
- **ES Modules**: 使用现代 JavaScript 模块系统

## 📝 版本

- yl-git-server: v1.0.0

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT
